name: notes_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔹 Pour charger les variables d’environnement (.env)
  flutter_dotenv: ^6.0.0

  # 🔹 SDK officiel Appwrite pour Flutter
  appwrite: ^13.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 🔹 Indique à Flutter d’inclure ton fichier .env dans le bundle
  assets:
    - .env
